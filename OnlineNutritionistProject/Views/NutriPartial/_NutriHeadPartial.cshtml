<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Diyetisyen Paneli</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
    <!-- FontAwesome for icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet" />
    <!-- Custom CSS -->
    <style>
        body {
            background-color: #f0f3f4;
            color: #333;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        header {
            background-color: #4caf50; /* Canlı yeşil */
            color: #fff;
            position: fixed;
            top: 0;
            left: 250px;
            width: calc(100% - 250px);
            z-index: 1000;
            padding: 10px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .header-title {
            color: #fff;
            font-size: 1.25rem;
            text-align: center;
            width: 100%;
        }

        .sidebar {
            background-color: #ffffff; /* Beyaz */
            color: #333;
            position: fixed;
            top: 0;
            left: 0;
            bottom: 0;
            width: 250px;
            padding-top: 20px;
            padding-bottom: 20px;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
        }

        .sidebar-heading {
            color: #4caf50; /* Canlı yeşil */
            margin-bottom: 1rem;
            text-align: center;
            font-weight: 600;
            font-size: 1.2rem;
            text-transform: uppercase;
        }

        .nav-link {
            color: #333;
            font-size: 1rem;
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 10px 15px;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

            .nav-link i {
                font-size: 1.2rem;
            }

            .nav-link:hover {
                background-color: #e8f5e9; /* Açık yeşil */
                color: #4caf50; /* Canlı yeşil */
                border-radius: 8px;
            }

        .nav-item {
            padding: 0.5rem 1rem;
        }

        .main-content {
            margin-left: 250px;
            padding: 70px 30px;
            background-color: #fff;
            min-height: 100vh;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .action-buttons {
            margin-top: auto;
            display: flex;
            flex-direction: column;
            gap: 15px;
            padding: 20px;
        }

            .action-buttons a {
                text-align: center;
                padding: 10px;
                border-radius: 5px;
                text-decoration: none;
                font-weight: 600;
                transition: background-color 0.3s ease;
            }

            .action-buttons .btn-site {
                background-color: #4caf50; /* Canlı yeşil */
                color: #fff;
            }

                .action-buttons .btn-site:hover {
                    background-color: #388e3c; /* Daha koyu yeşil */
                }

            .action-buttons .btn-logout {
                background-color: #f44336; /* Canlı kırmızı */
                color: #fff;
            }

                .action-buttons .btn-logout:hover {
                    background-color: #d32f2f; /* Daha koyu kırmızı */
                }

        footer {
            background-color: #f0f3f4; /* Açık gri */
            color: #333;
            text-align: center;
            padding: 10px 0;
            position: fixed;
            bottom: 0;
            left: 250px;
            width: calc(100% - 250px);
        }

        .search-bar {
            border: 1px solid #4caf50; /* Canlı yeşil */
            border-radius: 5px;
            padding: 5px;
            width: 250px;
            background-color: #fff;
            color: #333;
        }

            .search-bar::placeholder {
                color: #888;
            }

        .header-icons {
            display: flex;
            align-items: center;
            gap: 20px;
        }

            .header-icons a {
                color: #fff;
                font-size: 1.2rem;
            }

                .header-icons a:hover {
                    color: #e8f5e9; /* Açık yeşil */
                }

        .cert-status {
            font-weight: bold;
            text-align: center;
        }

            .cert-status.pend {
                color: #ff9800; /* Turuncu */
            }

            .cert-status.appr {
                color: #4caf50; /* Yeşil */
            }

            .cert-status.req {
                color: #f44336; /* Kırmızı */
            }
    </style>
</head>
