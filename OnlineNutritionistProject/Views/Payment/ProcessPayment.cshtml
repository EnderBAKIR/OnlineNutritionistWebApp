@model PaymentModelDto
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Premium Ödeme Sayfası - Diyetisyen Hizmetleri</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery.mask/1.14.16/jquery.mask.min.js"></script>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f4f8;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .container {
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            padding: 40px;
            width: 400px;
        }
        h1 {
            color: #2c3e50;
            text-align: center;
            margin-bottom: 30px;
        }
        .form-group {
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            color: #34495e;
        }
        input[type="text"],
        input[type="number"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #bdc3c7;
            border-radius: 5px;
            font-size: 16px;
        }
        .card-info {
            display: flex;
            justify-content: space-between;
        }
        .card-info input {
            width: calc(33% - 5px);
        }
        .checkbox-group {
            display: flex;
            align-items: center;
            margin-top: 20px;
        }
        .checkbox-group input {
            margin-right: 10px;
        }
        button {
            background-color: #27ae60;
            color: white;
            border: none;
            padding: 12px;
            width: 100%;
            font-size: 18px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #2ecc71;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Premium Ödeme</h1>
        <form method="post" asp-action="ProcessPayment" asp-controller="Payment" id="paymentForm">
            <div class="form-group">
                <label for="cardHolderName">Kart Sahibinin Adı</label>
                <input type="text" asp-for="CardHolderName" id="cardHolderName" required>
            </div>
            <div class="form-group">
                <label for="cardNumber">Kart Numarası</label>
                <input type="text" asp-for="CardNumber" id="cardNumber" required>
            </div>
            <div class="form-group card-info">
                <div>
                    <label for="expireMonth">Ay</label>
                    <input type="text" asp-for="ExpireMonth" id="expireMonth" required>
                </div>
                <div>
                    <label for="expireYear">Yıl</label>
                    <input type="text" asp-for="ExpireYear" id="expireYear" required>
                </div>
                <div>
                    <label for="cvc">CVC</label>
                    <input type="text" asp-for="Cvc" id="cvc" required>
                </div>
            </div>
            <div class="checkbox-group">
                <input type="checkbox" id="registerCard">
                <label for="registerCard">Kartımı kaydet</label>
            </div>
            <button type="submit">Ödemeyi Tamamla</button>
        </form>
    </div>

    @* <script>
        $(document).ready(function() {
            $('#cardNumber').mask('0000 0000 0000 0000');
            $('#expireMonth').mask('00');
            $('#expireYear').mask('00');
            $('#cvc').mask('000');

            $('#paymentForm').on('submit', function(e) {
                e.preventDefault();
                
                var paymentData = {
                    CardHolderName: $('#cardHolderName').val(),
                    CardNumber: $('#cardNumber').val().replace(/\s/g, ''),
                    ExpireMonth: $('#expireMonth').val(),
                    ExpireYear: $('#expireYear').val(),
                    Cvc: $('#cvc').val(),
                    RegisterCard: $('#registerCard').is(':checked') ? 1 : 0
                };

                console.log('Ödeme verileri:', paymentData);
                alert('Ödeme işlemi başarıyla tamamlandı!');
            });
        });
    </script> *@
</body>
</html>